# Demo Registry Environment Variables
# Local development - uses same Postgres instance as backend
DATABASE_URL=postgresql://admin:admin@localhost:5442/opencourier-registry

# For Render deployment, DATABASE_URL will be set via environment variable
PORT=3001
NODE_ENV=development
